{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "orgs": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9-]+$",
        "maxLength": 100
      },
      "minItems": 1
    }
  },
  "required": ["orgs"],
  "additionalProperties": false
}
